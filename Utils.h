#ifndef Utils_h
#define Utils_h

class Utils {
public:
  static float average(float* values, unsigned short numberOfValues);
  static float standardDeviation(float* values, unsigned short numberOfValues);
};

#endif